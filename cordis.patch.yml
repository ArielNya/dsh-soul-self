# dsh-soul-self — bundle patch layer
#
# Cordis id stays `soul-md` so the settings namespace and Web UI keep working.
# Package name is dsh-soul-self.
- insert:
    - id: soul-md
      name: 'dsh-soul-self'
